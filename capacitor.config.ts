import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'healingHeart2019',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
